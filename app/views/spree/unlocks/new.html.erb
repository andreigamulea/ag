<div class="max-w-md mx-auto mt-10">
  <h2 class="text-2xl font-bold mb-4">Resend unlock instructions</h2>
  <%= form_for(:spree_user, as: :spree_user, url: spree_user_unlock_path, html: { class: "space-y-4" }) do |f| %>
    <%= render "spree/shared/error_messages", resource: spree_user %>

    <div class="field">
      <%= f.label :email, class: "block text-sm font-medium text-gray-700" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500" %>
    </div>

    <div class="actions">
      <%= f.submit "Resend unlock instructions", class: "w-full bg-indigo-600 text-white py-2 rounded-md hover:bg-indigo-700" %>
    </div>

    <%= render "spree/shared/links" %>
  <% end %>
</div>